version: '3.8'

services:
  finops-optimizer:
    build: .
    ports:
      - "5001:5000"
    environment:
      - .:/app
    working_dir: /app
    command: ["python", "__main__.py", "--host", "0.0.0.0", "--port", "5000"]
